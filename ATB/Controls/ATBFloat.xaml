<UserControl x:Class="ATB.Controls.ATBFloat"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0" Orientation="Horizontal">
            <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=TextBlockValue}" 
                       Style="{DynamicResource ATBTextBlock}" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0"/>
            <TextBox Name="TextBox" 
                     Text="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Value, UpdateSourceTrigger=PropertyChanged}"
                     Width="80" 
                     Height="25"
                     VerticalContentAlignment="Center"
                     Template="{DynamicResource ATBTextBox}"
                     Style="{DynamicResource ATBTextBoxStyleRegular}"/>
        </StackPanel>
        
        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="5,0,0,0">
            <Button Name="ButtonIncrement" 
                    Width="20" 
                    Height="12" 
                    Content="▲" 
                    Style="{DynamicResource ATBButton}"
                    FontSize="8"/>
            <Button Name="ButtonDecrement" 
                    Width="20" 
                    Height="12" 
                    Content="▼" 
                    Style="{DynamicResource ATBButton}"
                    FontSize="8"/>
        </StackPanel>
    </Grid>
</UserControl> 